"""Recipes — notes-flavored recipes + URL import (personal-OS module #6).

The `recipes` table owns the truth. `importer.import_recipe` does an egress-guarded fetch + ONE
grammar inference (no loop). Cross-module: recipe → shopping list → tasks. CRUD is graded auto-run.
"""
