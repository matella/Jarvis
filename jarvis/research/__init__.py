"""Deep research — a bounded deterministic harness over one-shot inferences (personal-OS module #5).

`plan (1 inference) → fan-out web searches → synthesize (1 inference)`, bounded by per-depth caps.
NO step re-plans itself; NO inference decides to loop (Hard Rule #2). The run record is the harness
log; the report becomes a Document. See harness / models / repository / tools.
"""
