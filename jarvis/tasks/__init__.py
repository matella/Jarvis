"""Tasks — the to-do module (personal-OS user documents).

The `tasks` table is the source of truth (foundation Evolution #1); awareness events are notices.
CRUD tools are graded auto-run (low-risk + reversible + on-box). See models / repository / tools.
"""
