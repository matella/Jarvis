"""Notes — freeform markdown jottings (personal-OS user documents).

The `notes` table is the source of truth; recall is via the shared pgvector search hook. CRUD tools
are graded auto-run. Promoting a note → task / document is a cross-module action.
"""
